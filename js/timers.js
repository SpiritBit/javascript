'use strict'

window.addEventListener("load", () => {
    // TIMERS

/*
    // SetInterval. Ejecuta la función cada X milisegundos (3000)
    var tiempo = setInterval(() => {
        console.log("setInterval");
    }, 1000);

    */

    // SetTimeout. Se ejecuta una vez, pasado el tiempo
    var tiempo = setTimeout(() => {
        console.log("setTimeout");
    }, 3000);

    // Con "ClearInterval" eliminamos el Intervalo
});